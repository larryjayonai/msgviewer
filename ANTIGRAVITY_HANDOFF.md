# Antigravity 인계 — MSG Viewer

## 현재 상태

- 원래 작업 폴더: `C:\Users\LG\Documents\ChatGPT\msgviewer`
- 기준 Seed: `seed.yaml`, ID `seed_msgviewer_20260917`, 명세 수정 번호 **5**.
- 사람이 읽는 상세 명세: `SPEC.md`.
- 사용자 승인: 요구사항 인터뷰와 제안 채택을 완료했고 `ooo run`으로 구현을 요청했습니다. 실행 절차를 읽는 단계에서 중단한 뒤 모든 작업을 로컬에 저장하고 Antigravity에서 이어가도록 요청했습니다.
- **소스 구현, EXE 빌드, 실제 앱 테스트와 성능 측정은 아직 시작하지 않았습니다.** `dist/MsgViewer.exe`와 `reports/`의 실제 결과는 아직 없습니다.
- 호출 가능한 Ouroboros MCP 실행 도구가 현재 Codex 세션에 노출되지 않았습니다. 시작한 MCP 실행·작업·세션 ID가 없습니다. 존재하지 않는 실행을 resume하지 말고 저장한 Seed에서 새 실행을 시작합니다.
- 중단 직전 작업은 스킬·명세 읽기였습니다. 이 작업에서 외부 구현 워커나 빌드 프로세스를 시작하지 않았습니다. 인계 점검에서 관련 `python`, `uv`, `agy`, `MsgViewer`, C/C++ 빌드 프로세스는 관찰되지 않았습니다.
- Git 브랜치는 `master`이며 명세와 문서는 아직 커밋되지 않은 로컬 파일입니다. 파일 저장과 Git 커밋은 구분합니다. 기존 파일을 초기화하거나 삭제하지 마세요.

이 문서는 **확정 요구사항과 작업 상태를 보존한 인계 요약**입니다. 전체 대화·모든 도구 이벤트의 원문 내보내기를 가장하지 않습니다. 상세 결정 근거는 아래 수정 이력과 명세에 있습니다.

## 이어받는 순서

1. `seed.yaml`, `SPEC.md`, `docs/seed-audit.json`, 이 문서를 읽고 현재 폴더와 도구 환경을 확인합니다.
2. Antigravity에서 Ouroboros 실행 MCP 도구가 실제로 호출 가능하면 해당 런타임의 `ooo run` 절차로 `seed.yaml`을 실행합니다. 실행 도구가 없다면 그 사실을 알리고 같은 명세를 직접 구현할 수 있습니다. 설치 경로·도구 노출·모델 선택을 확인하지 않고 실행 ID나 백그라운드 작업을 만들었다고 보고하지 마세요.
3. x86 빌드 도구와 Windows 기본 구성에서 사용 가능한 MSG 파서·본문 렌더링 방식을 확인합니다. 구현 언어와 렌더러는 아직 선택하지 않았습니다.
4. 기능 구현 후 실제 x86 단일 EXE를 `dist/MsgViewer.exe`로 빌드합니다.
5. 실제 근거를 `reports/acceptance.md`와 `reports/performance.csv`에 남깁니다. 명세 QA 점수나 파일 존재만으로 실행 검증을 통과 처리하지 않습니다.

요구사항을 다시 처음부터 인터뷰할 필요는 없습니다. 구체적인 기술 선택은 승인된 조건 안에서 진행합니다. 불가능하거나 실제 충돌이 확인될 때만 필요한 결정을 설명합니다.

## 반드시 유지할 배포·성능 조건

- **x86 EXE 하나**로 Windows 10 x86·x64와 Windows 11 x64를 지원합니다. x64 전용의 이점을 논의했지만 사용자는 x86 유지를 명시했습니다.
- Outlook·별도 런타임·브라우저·언어팩 설치 없이 Windows 기본 구성에서 첫 실행부터 사용할 수 있어야 합니다.
- 앱과 앱이 호스팅한 렌더러는 **인터넷 요청 시도 자체를 하지 않습니다**. 원격 이미지·CSS·글꼴·스크립트, 업데이트 확인, 통계·오류 전송, 온라인 번역 및 링크를 통한 외부 브라우저 호출을 사용하지 않습니다.
- 단일 EXE **7~8MB 목표**이며 더 작아도 됩니다. 크기 판정은 8,000,000바이트 이하이고 실제 바이트·MB·MiB를 함께 보고합니다. 초과하면 목표 미달임을 명시합니다.
- 최초 실행·MSG 열기·종료 속도를 RAM 절약보다 우선합니다. 사용자가 제공하지 않은 시간 SLA·RAM 상한·최대 MSG 크기를 만들지 않습니다. 초기 구현 후 대표 파일과 기록된 시험 환경에서 측정합니다.
- 앱 장식용 애니메이션·블러·그림자·전환 효과·이미지를 제외합니다. 메일 본문의 서식·표·내부 이미지는 보존합니다.

## 확정 화면과 언어

```text
[열기] [닫기]                   [언어 / Language / Langue / 言語] [종료]

보내는 사람: …                    전송: YYYY-MM-DD HH:mm:ss
받는 사람: …
참조: …
숨은참조: …
제목: 길면 두 줄·세 줄 등으로 자동 줄바꿈

본문: 서식·표·내부 이미지와 선택 가능한 텍스트

첨부파일
  한 줄에 한 파일명 / 일반 첨부가 없으면 없음
```

- 한 번에 MSG 하나만 표시합니다. 받는 사람·참조·숨은참조는 각각 한 줄이며 긴 값도 전체 확인·선택·복사가 가능해야 합니다. 제목을 세 줄에서 강제로 자르지 않습니다.
- 여섯 메일 기본정보와 본문의 마우스 선택·우클릭 텍스트 복사를 지원합니다. 서식 복사는 필수가 아닙니다.
- MSG에 없는 숨은참조·전송 시각 등을 추측하지 않고 정보 없음을 표시합니다. 파일 수정 시각이나 현재 시각을 메시지 전송 시각으로 대체하지 않습니다. 전송 시각의 표현은 초 단위·24시간제이며 Windows 시간대 사용은 명세에 기록한 구현 기본값입니다.
- 언어 변경 버튼은 **`언어 / Language / Langue / 言語` 전체가 하나의 고정 버튼**입니다. UI 언어가 바뀌어도 번역하거나 네 버튼으로 분리하지 않습니다. 위치는 오른쪽 위 종료 버튼 바로 왼쪽입니다.
- 한국어·영어·프랑스어·일본어 번역 리소스를 EXE에 포함합니다. 매 실행 **Windows 표시 언어**가 ko/en/fr/ja이면 해당 UI, 그 외는 영어, 조회 실패는 한국어입니다. 날짜·숫자 지역 형식이 기준이 아닙니다.
- 수동 변경은 즉시 적용되고 현재 실행에서만 유지됩니다. 설정 파일·레지스트리에 언어를 저장하지 않습니다. 선택 항목은 `한국어 / English / Français / 日本語`입니다.
- Windows 기본 열기·저장 창의 OS 소유 문구는 Windows 표시 언어를 따릅니다. 메일 제목·주소·본문·첨부파일명 자체는 번역하지 않습니다.

## 본문·첨부·정리

- 일반 이메일 MSG를 우선 지원합니다. 지원 불가 일정·연락처·암호화·권한 보호 형식은 이유를 표시합니다. 온라인 인증이나 복호화 기능을 새로 요구하지 않습니다.
- 본문 삽입·서명·로고 이미지는 본문에 표시하고 일반 첨부 목록에서는 제외합니다. 일반 사진 파일 첨부는 목록에 남깁니다.
- 첨부 이름 클릭은 **다른 이름으로 저장만** 제공합니다. 대화상자의 기본 경로는 매번 현재 사용자의 실제 Windows 바탕화면 폴더입니다. 첨부 열기·실행·내부 미리보기는 제외합니다.
- 일반 파일 첨부는 원본 바이트를 보존합니다. 첨부된 MSG도 저장하며 저장소형 메일 첨부는 내용과 내부 첨부가 보존된 유효한 독립 MSG로 저장합니다.
- 일반 첨부 0개이면 하단 섹션에 현재 UI 언어로 없음에 해당하는 문구를 표시합니다. 본문 이미지만 있는 메일도 같은 기준입니다.
- 닫기는 현재 MSG와 그 임시자원을 정리하고 앱은 유지합니다. 종료·창 X는 앱 임시자원을 정리하고 프로세스를 끝냅니다. 비정상 종료 잔여물은 다음 실행에서 정리합니다.
- 사용자 저장 첨부·다른 앱이나 실행 중인 다른 인스턴스의 파일은 삭제하지 않습니다. 앱 임시파일·캐시·자체 열람기록이 정리 대상이며 Windows 자체 기록은 대상 밖입니다.

## 검증 상태를 정확히 구분하기

- 설치된 `ouroboros-ai 0.51.7` Seed 모델로 현재 YAML 검증을 수행했습니다. 11개 AC와 6개 ontology 필드가 있습니다.
- 최신 `docs/seed-qa.json`의 0.99/pass는 **수정 4→5 문서 변경에 대한 독립 로컬 모델 검토**입니다. 코드·GUI·네트워크·성능 테스트가 아닙니다.
- `docs/evaluation.md`와 `docs/evaluation-mechanical.json`은 수정 1의 역사적 평가입니다. 현재 명세 또는 제품의 공식 실행 통과로 재사용하지 않습니다.
- `ambiguity_score: 0.15`는 스키마 기본값입니다. MCP가 실제 측정한 수치로 표시하지 않습니다.
- Windows 10 x86, Windows 10 x64, Windows 11 x64는 각각 검증합니다. x64에서 x86 앱이 동작하는 것만으로 Windows 10 x86 OS 검증을 대체하지 않습니다.
- 연결이 가능한 환경에서도 앱과 렌더러의 DNS·연결 시도가 없는지 관찰합니다. 요청 실패나 방화벽 차단만으로 무통신 통과를 선언하지 않습니다.
- 시험하지 못한 OS·샘플·기능은 미검증으로 남깁니다. 최종 EXE 크기와 최초 실행·열기·종료 시간은 실제 측정합니다.

## 로컬 도구와 참고 정보

인계 시 실제 확인한 Antigravity CLI 경로는 `C:\Users\LG\AppData\Local\agy\bin\agy.exe`이며 `--help` 실행이 성공했습니다. `--prompt-interactive`가 초기 프롬프트와 함께 대화를 시작하는 옵션입니다. `Start-Antigravity.ps1`은 이 옵션을 사용하며 모델이나 권한 기본값을 덮어쓰지 않습니다.

기존 Ouroboros Python 경로는 `C:\Users\LG\AppData\Roaming\uv\tools\ouroboros-ai\Scripts\python.exe`, uvx 경로는 `C:\Users\LG\AppData\Roaming\Python\Python312\Scripts\uvx.exe`입니다. 현재 PATH에서 uv/uvx가 발견되지 않았으므로 필요한 경우 확인된 절대 경로를 사용합니다. 이번 인계에서 Antigravity의 MCP 연결이나 인증 상태를 실시간 검증하지 않았으므로 실행 시작 시 확인합니다.

이전에 Win32/C++와 OS 렌더러를 후보로 논의했지만 확정하지 않았습니다. 특정 렌더러가 모든 대상 Windows에서 조건을 충족한다고 가정하지 말고 실제로 검증합니다. 현재 빌드 도구 설치 여부는 구현 시작 전에 다시 조사해야 합니다.

## 보존한 이력

- `docs/revision-2.md`~`docs/revision-5.md`: 사용자 요청과 반영 차이.
- `docs/history/revision-1/`~`revision-4/`: 각 직전 명세·검토 기록.
- `docs/seed-audit.json`, `docs/seed-mechanical.json`: 최신 파일 해시와 검증 출처.
- `handoff/state.json`: 이번 인계 시점의 작업 상태.
- `handoff/manifest.json`: 저장 파일의 SHA-256 목록.
- `handoff/`의 ZIP: 인계 문서와 모든 프로젝트 산출물·이력의 스냅샷. `.git` 내부 정보나 전역 개인 설정은 포함하지 않습니다.
