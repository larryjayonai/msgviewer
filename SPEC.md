# 오프라인 MSG 뷰어 구현 명세

이 문서는 사용자가 승인한 요구사항의 구현 및 검증 지침이다. 기준 Seed는 `seed.yaml`이다. 현재 작업의 산출물은 명세이며 앱, EXE 또는 실측 결과가 이미 만들어졌다는 의미는 아니다.

수정 5: 기존 x86 공용 EXE와 화면 배치를 유지하며, 언어 변경 버튼은 UI 언어와 관계없이 `언어 / Language / Langue / 言語`라는 고정 문구를 한 버튼에 표시한다. 이 단계까지를 버전 1.0 (v1.0)으로 정의한다.

수정 6 (버전 1.1 / v1.1): 단축키 F2(열기), F4(닫기), F8(언어 순환)을 지정하고 모든 언어 버튼 명칭에 `(F#)`을 표시한다. 언어 버튼은 마우스 클릭 시 4개 언어 선택 메뉴를 제공하되 메뉴 닫힘 시 눌림(sunken) 상태를 정상 복원하며, F8 키보드 입력 시 한국어→영어→프랑스어→일본어로 순환 전환한다. 메일의 받는 사람 또는 제목이 비어 있는 경우 첨부파일 없음과 동일하게 언어별 `없음`(`None`, `Aucun`, `なし`)으로 표시한다.

수정 7 (버전 1.2 / v1.2): 파일 크기 8 MiB(8,388,608바이트) 한도를 유지하면서, 메일 본문의 표시 안정성과 Windows 10/11 전 환경(32비트, 레거시 빌드, 오프라인 폐쇄망) 호환성을 극대화하기 위한 '무결점 본문 렌더링 11대 기능', '30~40MB+ 대용량 MSG 메모리 안전 대책', 그리고 '4단계 상세 진단 에러 리포팅'을 추가한다. 기술적 구현 방식을 두 가지 대안(**v1.2-revA**: .NET 4.0 하위 호환 C# 단일 EXE, **v1.2-revB**: 순수 C Win32 `/MT` 네이티브 단일 EXE)으로 이원화하여 명세한다.

---

## 1. 제품과 배포

Windows 10 x86·x64와 Windows 11 x64에서 같은 x86 EXE 하나로 동작하는 휴대용 MSG 뷰어를 만든다. Windows 11의 32비트 OS는 지원 대상으로 가정하지 않는다. 사용자 PC에는 Outlook, 별도 런타임, 브라우저 또는 다른 구성요소를 설치할 필요가 없어야 하며 첫 실행도 인터넷 없이 가능해야 한다. Windows 기본 구성요소 사용은 허용한다.

인터넷이 연결된 PC에서도 앱과 앱이 호스팅한 본문 렌더러는 인터넷 요청을 일체 시도하지 않는다. 
- 본문 내 원격 이미지(`http://`, `https://`), 원격 CSS, 웹폰트, 스크립트 요청을 파싱 단계에서 원천 차단하고 오프라인 플레이스홀더로 대체한다.
- 업데이트 확인, 사용 통계, 오류 전송, 온라인 번역, 언어팩 다운로드를 수행하지 않는다. 
- 메일 내 외부 링크를 눌러 웹 브라우저를 실행하거나 외부 통신으로 넘기는 동작도 제공하지 않는다. 
- 표시용 원문과 텍스트 선택·복사는 유지한다. 앱이 유발한 통신과 Windows의 독립적인 백그라운드 통신은 구분하며, OS 전체의 통신을 끄라는 요구로 확대하지 않는다.

EXE의 목표 크기는 7~8MB(최대 8,388,608바이트) 이내다. 
- **v1.2-revA** 채택 시: 약 180 ~ 250 KB (8 MiB의 약 3%)
- **v1.2-revB** 채택 시: 약 600 KB ~ 1.5 MB (8 MiB의 약 15%)
두 안 모두 8MB 대비 극히 여유 있게 배포한다.

### 대용량 파일(30~40MB+) 메모리 안정성 목표
32비트(x86) 환경의 최대 2GB 가상 메모리 주소 한계 속에서 30~40MB 이상의 대용량 MSG 파일을 열더라도 OOM(Out of Memory)이나 메모리 누수가 발생하지 않도록 지연 스트리밍(Lazy Streaming) 구조를 채택한다. 대용량 첨부파일 데이터를 메모리에 일괄 적재하지 않으며, 열람 중 프로세스 RAM 점유율을 30MB 이하로 억제한다.

---

## 2. 사용 흐름 및 상세 에러 리포팅

| 동작 | 결과 |
| --- | --- |
| 실행 | 프로세스 레벨 IE11 에뮬레이션 확인/등록 후 빈 뷰어 표시. 최상단 왼쪽에 열기 (F2)·닫기 (F4), 오른쪽에 언어 (F8)·종료 버튼 표시 |
| 열기 버튼 클릭 또는 F2 키 | 파일 선택 창에서 MSG 하나를 선택해 표시 |
| 파일 로드 실패 시 | **4단계 상세 에러 진단 대화상자** 표시 (실패 단계, 파일 경로, 오류 유형, 기술 세부정보, 조치 방법) |
| 다른 MSG 열기 | 같은 화면의 현재 문서를 교체하고 이전 문서의 임시자원 및 메모리 즉시 정리 |
| 닫기 버튼 클릭 또는 F4 키 | 현재 MSG와 임시자원을 정리하고 앱은 빈 상태로 유지 |
| 종료 / 창 X | 앱이 만든 임시자원을 정리한 후 프로세스 종료 |
| 본문 선택 → 우클릭 복사 | 선택한 텍스트를 클립보드로 복사 |
| 메일 기본정보 선택 → 우클릭 복사 | 보내는 사람·전송 일시·받는 사람·참조·숨은참조·제목의 선택한 값을 텍스트로 복사 |
| 첨부파일 이름 클릭 | 바탕화면을 기본 위치로 하는 다른 이름으로 저장 창 표시 (64KB 청크 단위 지연 스트리밍 저장) |
| 본문 줌 (Ctrl + 휠 또는 Ctrl +/-) | 본문 크기를 10% 단위로 확대/축소 (Ctrl+0 입력 시 100% 원래 크기로 즉시 초기화) |
| 원격 이미지 호버 | 오프라인 차단 플레이스홀더에 마우스 호버 시 툴팁으로 원본 주소 및 설명(alt) 표시 |
| 언어 버튼 클릭 | 한국어·영어·프랑스어·일본어 4개 선택 메뉴를 표시하여 즉시 적용하며, 메뉴 닫힘 시 버튼 눌림 상태를 정상 복원 |
| F8 키 입력 | 메뉴 팝업 없이 한국어 → 영어 → 프랑스어 → 일본어 → 한국어로 즉시 순환 전환 |

### 4단계 상세 에러 안내 구조
파일 열람에 실패할 경우 사용자가 기술 원인을 명확히 파악할 수 있도록 대화상자에 아래 포맷으로 안내한다:
```text
[메일 열기 실패 안내]
• 실패 단계: [1~4단계 중 해당 단계 명시]
• 대상 파일: C:\path\to\sample.msg
• 오류 유형: 파일 손상 / 접근 거부 / 시그니처 불일치 등
• 기술 상세: 구체적인 예외 메시지 또는 파서 내부 진단 코드
• 조치 방법: 권장 해결책 안내
```

- **1단계 (파일 열기 및 I/O)**: 권한 부족, 공유 위반(다른 프로세스 잠금), 0바이트 파일
- **2단계 (OLE 복합 파일 구조 분석)**: 매직 헤더(`0xD0CF11E0`) 불일치, FAT 체인 손상
- **3단계 (MAPI 속성 및 본문 추출)**: 필수 속성 스트림 누락, 압축 RTF 헤더 오류, 문자셋 디코딩 실패
- **4단계 (본문 렌더링)**: MSHTML DOM 생성 오류 (자동 RichTextBox Fallback 안내)

---

## 3. 본문과 첨부 (무결점 렌더링 11대 기능 & 메모리 안전)

### 3.1 헤더 영역 6종 정보
보내는 사람·전송 일시·받는 사람·참조·숨은참조·제목의 여섯 항목을 GUI 배치대로 표시한다.
- 빈 받는 사람(To) 또는 제목(Subject)은 언어별 `없음`(`None`, `Aucun`, `なし`)으로 표시한다.
- 전송 일시는 `YYYY-MM-DD HH:mm:ss` 24시간제로 표시한다.

### 3.2 무결점 본문 렌더링 11대 세부 명세
1. **IE11 표준 모드 에뮬레이션 및 오프라인 리셋 CSS (`IE=Edge`)**
2. **순수 RTF(Rich Text Format) → HTML 변환 엔진 내장**
3. **오프라인 HTML Sanitizer 및 손상 태그 스택 자동 복원**
4. **레거시 다국어 인코딩 자동 감지 (Charset Fallback)**
5. **첨부파일 형식별 Hi-DPI 아이콘 및 영역 가독성 강화**
6. **MAPI 코드페이지(`PR_INTERNET_CPID`, `PR_MESSAGE_CODEPAGE`) 우선 파싱**
7. **하이브리드 인라인 이미지 리졸버 (빨간 X박스 박멸)**
8. **프로세스 레벨 `FEATURE_BROWSER_EMULATION` 자동 등록**
9. **본문 줌 컨트롤(Ctrl+휠) 및 시스템 폰트 스택 대응**
10. **렌더러 장애 시 무결점 Fallback 안전망 (`RichTextBox` / `RichEdit`)**
11. **원격 이미지 오프라인 안전 차단 및 플레이스홀더(`[ 🖼️ 외부 이미지 (오프라인 차단됨) ]`) 표시**

### 3.3 대용량 첨부파일 지연 스트리밍 저장 (Lazy Streaming)
- 30~40MB+ 크기의 대용량 첨부는 파싱 단계에서 바이너리 바이트를 메모리에 로드하지 않고 파일 내 오프셋과 크기만 유지한다.
- 사용자가 저장을 요청할 때만 파일 스트림에서 직접 64KB 버퍼 청크 단위로 대상 파일에 기록하여 x86 메모리 낭비를 원천 차단한다.
- 본문 인라인 이미지는 Base64 Data URI로 변환된 즉시 원본 바이트 배열을 파기하여 LOH(Large Object Heap) 메모리 파편화를 방지한다.

---

## 4. 아키텍처 및 구현 대안 명세 (revA vs revB)

### 4.1 [v1.2-revA] .NET Framework 4.0 하위 호환 C# 아키텍처
- **기술 스택**: C# (.NET Framework 4.0 / CLR 4.0)
- **빌드 도구**: Windows 기본 내장 `C:\Windows\Microsoft.NET\Framework\v4.0.30319\csc.exe`
- **호환성 보장**:
  - Windows 10의 모든 빌드(2015년 최초 빌드 10240부터 최신 22H2까지) 및 Windows 11 전 기종에 CLR 4.0이 100% 기본 활성화되어 있으므로, 4.8이 없는 폐쇄망 32비트 윈도우 10에서도 다운로드 팝업 없이 즉시 실행.
- **메모리 보호**: 대용량 첨부 `Stream` 청크 복사, `GC.Collect()` 명시적 리소스 반환.
- **예상 크기**: 약 180 ~ 250 KB

### 4.2 [v1.2-revB] 순수 C (Win32 API + 정적 런타임 `/MT`) 아키텍처
- **기술 스택**: Pure C (C99/C11) + Win32 API + OLE/COM In-Place Container
- **빌드 도구**: Microsoft Visual C++ `cl.exe` (`/MT`) 또는 MinGW-w64 `gcc`
- **호환성 보장**:
  - .NET Framework의 존재 여부와 100% 무관하게 동작.
  - .NET이 손상/삭제된 윈도우, Windows PE(사전 설치 복구 환경)에서도 100% 실행 보장.
- **5대 메모리 누수 및 안정성 방어 규약**:
  1. **아레나 메모리 할당자 (Arena Allocator)**: 메일 열람당 8MB 가상 메모리 블록을 할당하고 개별 `malloc`/`free`를 금지하며 포인터 전진 방식으로만 메모리를 할당. 메일 닫기(`F4`) 또는 교체 시 `Arena_Reset()` 단 1회 호출로 이전 메일의 모든 힙 메모리를 통째로 회수하여 누수율 0% 보장.
  2. **MSHTML 윈도우 전역 단일화**: 앱 실행 시 단 하나의 MSHTML WebBrowser 윈도우만 영구 생성하고, 메일 교체 시 `IHTMLDocument2::write`로 내용만 갱신. COM 인스턴스 생성/소멸 사이클을 원천 제거하여 COM 참조 카운트(`AddRef`/`Release`) 누적 누수를 물리적으로 차단.
  3. **메모리 맵 파일(Memory-Mapped Files) 하이브리드**: `CreateFileMappingW(hFile)` + `MapViewOfFile`을 통해 디스크 파일 자체를 백킹 스토어로 직접 매핑. 페이징 파일(가상 메모리)이 0MB로 설정된 환경에서도 100% 정상 동작하며, 매핑 실패 시 64KB 청크 슬라이딩 `ReadFile`로 자동 Fallback.
  4. **단일 탈출구(`goto cleanup`) 및 `SAFE_RELEASE` 매크로**: 모든 파서 함수의 에러 처리를 단일 정리 블록으로 일원화하고 COM 포인터 안전 해제 매크로 강제.
  5. **Win32 안전 문자열 API 강제**: `StringCchCopyW`, `StringCchPrintfW` 및 엄격한 바운드 검사를 통해 손상된 MSG 파일로 인한 버퍼 오버런 크래시를 원천 방어.
- **예상 크기**: 약 600 KB ~ 1.5 MB

---

## 5. 검증 계획

구현 완료 후 다음 항목을 검증한다:
1. **플랫폼 및 단일 바이너리 크기 (8 MiB 이하)**
2. **완전 오프라인 및 원격 리소스 차단 검증**
3. **11대 본문 렌더링 무결성 검증**
4. **대용량 파일(30~40MB) 열람 및 메모리 안정성 검증**:
   - 30MB 이상 대용량 첨부를 가진 샘플 로드 시 프로세스 메모리가 30MB 이하로 유지되는지 확인.
   - 대용량 첨부파일의 바탕화면 청크 스트리밍 저장 및 바이트/해시 일치 확인.
5. **4단계 상세 에러 진단 리포팅 검증**:
   - 잘못된 포맷(%PDF 헤더 등) 및 손상된 파일 로드 시 정확한 실패 단계와 기술 상세 메시지가 팝업되는지 확인.
